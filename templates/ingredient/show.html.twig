{% extends 'base.html.twig' %}

{% block title %}Hello DifficulteController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    
    <p> <a href="{{ path('ingredient.list') }}"> Retour</a></p>

{% if ingredient is not empty %}

      {{ ingredient.nomIngredient }} 

 <a href="{{ path('ingredient.edit', { id: ingredient.idIngredient }) }}" class="btn btn-secondary btn-sm">Modifier</a>      
   {% endif %}
</div>
{% endblock %}
